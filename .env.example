# 项目环境变量模板
# 复制此文件为 .env.local 进行本地开发，复制为 .env 用于生产环境

# Better Auth 配置
# 生成方式: openssl rand -base64 32 或在 Linux 上使用 head -c 32 /dev/urandom | base64
BETTER_AUTH_SECRET=your-secret-key-at-least-32-characters-long

# 应用 URL（部署时改为您的域名或服务器 IP）
BETTER_AUTH_URL=http://localhost:3000

# 如果需要在客户端使用 app URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
