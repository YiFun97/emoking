目前我有一个阿里云轻量云服务器，我决定拿来写一些项目

这个项目会有多个前端页面，每个path的业务是不一样的，能通过 path 访问不同的前端页面，比如可能 www.tets.cn/admin 是一个管理页面，www.tets.cn/blog 是一个博客页面 或者 电商页面等等

技术栈： Next.js 15 + shadcn ui + drizzle ORM + tailwindcss + typescript + Better Auth + mysql

项目会有登录鉴权系统，可以游客态访问，也会有右上角按钮点击登录访问，支持普通账号注册，比如用户自定义账号和密码（需要二次确认），后续会支持手机号登录，但本期先不做手机号登录


服务器环境：阿里云的2核2g3m + docker 

这是一个空项目，今天的任务我希望你根据整体背景搭建好前后端的架构，前端只需要展示一个 hello world 的文案。最终我能本地跑通并部署到阿里云服务器上

我只是买了一个云服务器，没用过 docker, 不知道怎么部署这个项目，请你实现完这个项目后，写一个文档告诉我怎么部署上去
